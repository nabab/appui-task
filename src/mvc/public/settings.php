<?php
/*
 * The idea is:
 * - to do a tree with the tasks' types
 * - to do another tree with the user and groups
 * - to attribute task types to default user and groups
 * - to create new types wherever in the tree
 * - to have task types created
 *
 **/

/** @var $this \bbn\mvc\controller */
$this->combo(_("Settings"), true);